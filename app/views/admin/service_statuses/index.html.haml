- model_class = ServiceStatus
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize.titleize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:name)
      %th= model_class.human_attribute_name(:permalink)
      %th= model_class.human_attribute_name(:color)
      %th= model_class.human_attribute_name(:status_type)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @service_statuses.each do |service_status|
      %tr
        %td= link_to service_status.id, admin_service_status_path(service_status)
        %td= service_status.name
        %td= service_status.permalink
        %td= service_status.color
        %td= service_status.status_type
        %td
          = link_to t('.show', :default => t("helpers.links.show")), admin_service_status_path(service_status), :class => 'btn btn-default btn-xs'
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_admin_service_status_path(service_status), :class => 'btn btn-default btn-xs'

= link_to t('.new', :default => t("helpers.links.new")), new_admin_service_status_path, :class => 'btn btn-primary'
